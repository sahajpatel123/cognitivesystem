"use client";

export default function SessionCommandSurfacePage() {
  return (
    <div className="surface-stack">
      <div className="page-header">
        <span className="status-strip">Session command surface</span>
        <h2>This page is not available in the MVP build.</h2>
        <p>Session composition UI is disabled to keep the production build stable.</p>
      </div>
    </div>
  );
}
