[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cognitivesystem"
version = "0.1.0"
description = "Decision-aware cognitive system"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
include = ["mci_backend", "backend"]
